#! /bin/sh

# @configure_input@

echo -n " -LPREFIX/lib -lpsrmore -lpsrbase -lpsrutil "

echo -n " @FFT_LIBS@ @SLA_LIBS@ @CFITSIO_LIBS@ "

for arg in $* ; do

case $arg in

pgplot)
  echo -n " @PGPLOT_LIBS@ "
;;

qt)
  echo -n " @QT_LIBS@ "
;;

esac

done
